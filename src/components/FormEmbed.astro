---
export interface Props {
	href: string | URL;
}

const { href } = Astro.props as Props;
---

<iframe class="border border-copy border-opacity-10 rounded-lg w-full h-[700px] bg-white" src={`${href}`} loading="lazy">Loading…</iframe>
